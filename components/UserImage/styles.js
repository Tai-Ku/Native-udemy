import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  userImageContainer: {
    height: 60,
    width: 60,
    backgroundColor: '#ccc',
    borderRadius: 50,
    borderColor: 'pink',
    borderWidth: 1,
    padding: 3,
  },
});
